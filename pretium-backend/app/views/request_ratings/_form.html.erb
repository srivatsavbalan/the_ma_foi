<%= form_for(request_rating) do |f| %>
  <% if request_rating.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(request_rating.errors.count, "error") %> prohibited this request_rating from being saved:</h2>

      <ul>
      <% request_rating.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student_id %>
    <%= f.number_field :student_id %>
  </div>

  <div class="field">
    <%= f.label :skill_id %>
    <%= f.number_field :skill_id %>
  </div>

  <div class="field">
    <%= f.label :teacher_id %>
    <%= f.number_field :teacher_id %>
  </div>

  <div class="field">
    <%= f.label :status %>
    <%= f.text_field :status %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
